# ============================================================
# Nginx 负载均衡配置 - 最少连接 (Least Connections) 算法
# ============================================================
#
# 算法说明：
#   最少连接算法会将请求分配给当前活跃连接数最少的后端服务器。
#   相比轮询，它能更智能地分配负载，特别适合请求处理时间差异
#   较大的场景。
#
# 特点：
#   - 考虑服务器当前负载（连接数）
#   - 适合请求处理时间不均的场景
#   - 能避免将请求发送到繁忙的服务器
#   - 对于长连接或慢请求场景效果更好

upstream tomcat_cluster {
    # 最少连接 (least_conn) 算法
    least_conn;

    server 172.28.0.11:8081 weight=1 max_fails=3 fail_timeout=30s;
    server 172.28.0.12:8082 weight=1 max_fails=3 fail_timeout=30s;
    server 172.28.0.13:8083 weight=1 max_fails=3 fail_timeout=30s;
}
